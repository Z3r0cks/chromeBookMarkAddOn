namespace BookmarkExtension {
   export class URL {
      title: string
      url: string

      constructor(title: string, url: string) {
         this.title = title;
         this.url = url;
      }
   }
}